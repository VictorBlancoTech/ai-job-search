# CLAUDE.md

Este archivo existe solo por compatibilidad con herramientas que lo buscan por convención.

**La fuente de verdad canónica de este workspace es [AGENTS.md](AGENTS.md).**

- Perfil del candidato: `perfil/` (01 está gitignored — datos personales)
- Workflows: `.opencode/commands/` (`/setup`, `/apply`)
- CV: Awesome-CV en `cv/` (masters gitignored, plantilla en `cv/plantilla/`)
